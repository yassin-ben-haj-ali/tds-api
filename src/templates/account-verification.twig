<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{% block title %}Verify Your Email{% endblock %}</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background: #f9f9f9;
        font-family: 'Montserrat', Arial, sans-serif;
      }
      .wrapper {
        width: 100%;
        background: #f9f9f9;
        padding: 30px 0;
      }
      .container {
        max-width: 520px;
        background: #ffffff;
        margin: auto;
        border-radius: 10px;
        padding: 30px 28px;
        box-shadow: 0 2px 16px rgba(0, 0, 0, 0.08);
      }
      .logo {
        max-width: 180px;
        margin: 0 auto 20px auto;
        display: block;
      }
      .image {
        max-width: 100%;
        margin: 20px auto;
        display: block;
      }
      .title {
        font-size: 20px;
        font-weight: bold;
        color: #1c57a4;
        margin-bottom: 10px;
        text-align: left;
      }
      .text {
        font-size: 14px;
        color: #444;
        line-height: 1.6;
        margin-bottom: 28px;
        text-align: left;
      }
      .btn-container {
        text-align: center;
        margin: 30px 0;
      }
      .btn {
        background: #1c57a4;
        color: #ffffff !important;
        padding: 12px 28px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: bold;
        font-size: 14px;
        display: inline-block;
      }
      .btn:hover {
        background: #194a83;
      }
      .fallback {
        background: #f2f2f2;
        padding: 10px;
        font-size: 13px;
        color: #333;
        border-radius: 6px;
        word-break: break-all;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #999;
        margin-top: 25px;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="container">

        {% if logo_url %}
          <img src="{{ logo_url }}" alt="Logo" class="logo" />
        {% endif %}

        <p class="title">Hello {{ user|default('there') }},</p>

        <p class="text">
          Thank you for creating an account!  
          Please verify your email address to activate your account and start using our service.
        </p>

        <div class="btn-container">
          <a href="{{ url }}" class="btn">
            Verify my email
          </a>
        </div>

        <p class="text" style="margin-bottom: 10px;">
          If the button above does not work, copy and paste this link into your browser:
        </p>

        <div class="fallback">{{ url }}</div>

        <div class="footer">
          If you did not create this account, you can safely ignore this message.<br />
          © {{ "now"|date("Y") }} Your Company Name.
        </div>

      </div>
    </div>
  </body>
</html>
